apiVersion: v1
kind: Service
metadata:
  name: cpulse-web
  namespace: conversion-pulse
spec:
  type: ClusterIP
  selector:
    app: cpulse-web
  ports:
    - name: http
      port: 80
      targetPort: 3000
