apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: conversion-pulse

resources:
  - ../../base
  - api-service-nodeport.yaml

images:
  - name: conversion-pulse/web
    newTag: "latest"
