apiVersion: v1
kind: Service
metadata:
  name: cpulse-web-nodeport
  namespace: conversion-pulse
spec:
  type: NodePort
  selector:
    app: cpulse-web
  ports:
    - name: http
      port: 80
      targetPort: 3000
      nodePort: 30984
